package space.themelon.eia64

import space.themelon.eia64.signatures.Sign

object EiaTraceTest {
    @JvmStatic
    fun main(args: Array<String>) {
        //val trace = EiaTrace()
        //trace.declareVariable(false, "cat_name", Sign.STRING)
        //trace.declareFn("animate", listOf("word" to Sign.STRING))
    }
}