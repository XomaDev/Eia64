fn generateSpaces(n: Int) {
    println("N is " + n)
    var spaces: String = ""
    var i: Int = 0
    until (i++ != n) {
        spaces = spaces + " "
    }
    return spaces
}

let num: Int = 5
println("Spaces: " + generateSpaces(num))