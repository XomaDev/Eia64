; Structure that holds an element and it's priority

var element: Any = ""
var priority = 0

fn init(_element: Any, _priority: Int) {
  element = _element
  priority = _priority
}

visible fn element(): Any = element

visible fn priority(): Int = priority

fn string(): String {
  return "(" + priority + ", " + element + ")"
}