; a class that can be included by others

var name = "Unknown"
var lastname = "Yo "

fn init(_name: String, _lastname: String) {
  name = _name
  lastname = _lastname
}

visible fn hello() {
  println(format("Hello %s!", name))
}