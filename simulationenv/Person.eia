let personName = ""

fn init(name: String) {
  personName = name
}

; fn to return field, normally another classes cannot access fields
fn getName() = personName

fn string() = "Person(" + personName + ")"